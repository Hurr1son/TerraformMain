apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    env  : {{ .Values.environment}}
spec:
  selector:
    project: {{ .Values.environment}}
  ports:
    - name      : listener
      protocol  : TCP
      port      : 80  
      targetPort: 8080
  type: LoadBalancer